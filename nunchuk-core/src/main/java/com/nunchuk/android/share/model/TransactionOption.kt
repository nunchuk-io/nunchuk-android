package com.nunchuk.android.share.model

enum class TransactionOption {
    CANCEL,
    EXPORT_PSBT,
    EXPORT,
    EXPORT_PASSPORT,
    IMPORT_KEYSTONE,
    IMPORT_PASSPORT,
    REPLACE_BY_FEE,
    COPY_TRANSACTION_ID,
    REMOVE_TRANSACTION,
}
