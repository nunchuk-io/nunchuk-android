<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/check_sign_message_navigation"
    app:startDestination="@id/checkSignMessageFragment">

    <fragment
        android:id="@+id/checkSignMessageFragment"
        android:name="com.nunchuk.android.main.components.tabs.services.keyrecovery.checksignmessage.CheckSignMessageFragment"
        android:label="CheckSignMessageFragment">
        <argument
            android:name="wallet_id"
            app:argType="string" />

        <argument
            android:name="user_data"
            app:argType="string" />

        <argument
            android:name="required_signatures"
            app:argType="integer" />
    </fragment>
</navigation>